
# Mount and edit Rasperry Pi Images

FROM ubuntu:17.04

RUN apt-get update && apt-get install -y --no-install-recommends \
  ca-certificates curl git vim less 

# mount /boot

# copy wpa_supplicant.conf

# copy ssh

# mount /etc

# edit system name

# set pi password



